{
  "name": "applock",
  "version": "1.0.0",
  "description": "App Lock with React Native UI and Kotlin Native Modules",
  "main": "index.js",
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "build-bundle": "node scripts/build-rn-bundle.js",
    "build-android": "npm run build-bundle && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-native": "^0.82.0",
    "@react-native-community/cli": "^20.0.2"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-native": "^0.82.0",
    "typescript": "^5.0.0",
    "metro-react-native-babel-preset": "^0.77.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
